apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bpp.fullname" . }}-test-connection"
  labels:
    {{- include "bpp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bpp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
